<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+: system splash = brand colour, padded logo, wordmark at the bottom. -->
    <style name="Theme.KJInvoice" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_bg</item>
        <!-- Light icons on the indigo splash; the app sets the right colours once running. -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_logo_animated</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/splash_branding</item>
    </style>
</resources>
